<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
    http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
        <property name="driverClassName" value="${rebate.db.driver}" />
        <property name="url" value="${rebate.db.url}" />
        <property name="username" value="${rebate.db.username}" />
        <property name="password" value="${rebate.db.password}" />
        <!-- <property name="timeBetweenEvictionRunsMillis" value="${rebate.db.timeBetweenEvictionRunsMillis}"/>
        <property name="minEvictableIdleTimeMillis" value="${rebate.db.minEvictableIdleTimeMillis}"/> -->
    </bean>

</beans>